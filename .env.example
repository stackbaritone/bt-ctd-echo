# Support Contact Configuration
# Email address for receiving support requests via FormSubmit.co
# You must verify this email at https://formsubmit.co before it will work
VITE_SUPPORT_EMAIL=echo-info@bt-tb.ca

# Optional: Custom form submission endpoint (replaces legacy FormSubmit)
# Recommended: Formspree (create a form and use its endpoint URL)
# Example: VITE_SUPPORT_FORM_ENDPOINT=https://formspree.io/f/yourFormId
# You can also point to your own serverless endpoint that accepts POST JSON.
VITE_SUPPORT_FORM_ENDPOINT=
